@model db.view.bks_BookStockDetail
@{
    ViewBag.Title = "入库明细表表";
    //wzrui 2020-02-19
}

<style type="text/css">
</style>
<script type="text/javascript">
        //刷新当前页面的数据
        function refleshData(exeCountSql) {
            $("#container").pager("reflesh", exeCountSql);
        }
        //初始化分页
        $(function () {
            $("#container").pager();
        });
</script>

@{ string ctrl = ViewContext.RouteData.Values["controller"].ToString(); }
<div class="container" id="container" data-width="@Model.PageWidth" data-url="/admin/bks_BookStockDetail/Select">
    <div class="search">
        <span>@Html.HiddenFor(a => a.stockCode)</span>
        <span>@Html.HiddenFor(a => a.opMode)</span>
    </div>
    @*<div class="toolbar">
        <a class="opSearch">查询</a>
        @Html.ActionLink("新增", "Insert", new { }, new { data_show = @rui.privCtl.isPriv(ctrl, rui.privCtl.eOp.Insert), @class = "showDialogR opInsert" })
    </div>*@
    <div class="pager" style="width: 90%; margin-left: auto; margin-right: auto; margin-top: 0px; margin-bottom: 0px;"></div>
    <div class="showData cbxCol">
        @{Html.RenderPartial("_ShowData" + Model.opMode);}
    </div>
</div>